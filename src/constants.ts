import * as path from 'path';

export default Object.freeze({
    currentSdEditVersion: '4.2-beta9',
    sdeditInstallDirectory: path.resolve(__dirname, '..', 'sdedit-bin'),
    sdeditFilename: 'sdedit.jar'    
});